#!/bin/sh
#PACKAGE_ROOT=$(cd -P -- "$(dirname -- "$0")/.." && pwd -P)
#eval \"${PACKAGE_ROOT}/bin/docker-run\" ./vendor/bin/codecept "${@}"
#!/usr/bin/env bash
docker-compose exec -w /var/www/html plugin ./vendor/bin/codecept "${@}"
